#include <stdio.h>
#include <stdlib.h>

/**
 * main- Prints numer zer to ten.
 *
 * Return: Always 0.
 */

int main(void)
{
	int n = 0;

	for (n = 0; n < 10; n++)
	printf("%d", n);
	putchar('\n');

	return (0);
}
